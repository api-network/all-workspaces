---
layout: none
---
{% for workspace in site.all %}{
   "title": "{{ workspace.name | strip_html | escape | strip }}",
   "description": "{{ workspace.description | strip_html | escape | strip | strip_newlines }}",
   "link": "{{ workspace.image }}",
   "apis": {{ workspace.apis | strip_html | escape | strip }},
   "collection": "{{ post.collection | strip_html | escape | strip }}",
   "updated": "{{ page.updated | strip_html | escape | strip }}"
}{% if forloop.last == true %}{% else %},{% endif %}{% endfor %}]
